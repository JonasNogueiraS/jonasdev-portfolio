import type { CurationMap } from "@/services/curation";

/**
 * Curadoria padrão do portfólio. Enquanto vazia, o site destaca
 * automaticamente os repositórios mais recentes (não-forks) do GitHub.
 */
export const defaultCuration: CurationMap = {};
